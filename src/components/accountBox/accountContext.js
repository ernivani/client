import { createContext } from "react";

export const AccountContext = createContext();

// AccountContext est un objet qui contient deux propriétés : Provider et Consumer.
// Provider est un composant qui permet de fournir des données à tous les composants qui sont dans son arbre.
// Consumer est un composant qui permet de consommer les données fournies par le Provider.
